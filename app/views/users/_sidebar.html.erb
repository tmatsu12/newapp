<h2 class="mb-3">ユーザー情報</h2>
<% if user_signed_in? %>
  <%= link_to user_path(user) do %>
    <%= attachment_image_tag user, :profile_image, class:"rounded-lg", fallback: "no_image.png", size: "150x150" %>
  <% end %>
  <p class="mt-2">名前：<%= user.name %></p>

  <% if user.prefecture2 %>
    <p>住んでいる（いた）県：<%= user.prefecture2.name %></p>
  <% else %>
    <p>住んでいる（いた）県：未入力</p>
  <% end %>

  <% if user.prefecture1 %>
    <p>住んでみたい県：<%= user.prefecture1.name %></p>
  <% else %>
    <p>住んでみたい県：未入力</p>
  <% end %>

<% else %>
  <p>まだログインしていません</p>
<% end %>

<!--sidebar・・・regionページのサイドバー-->
<!--sidebar1との違い・・・regionページには新規投稿ボタンをつけなかった-->

<table class="table table-striped table-hover mt-4">
  <thead>
    <tr>
      <td><h3>Region</h3></td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= link_to "北海道地方", "/region/hokkaido" %></td>
    </tr>
    <tr>
      <td><%= link_to "東北地方", "/region/tohoku" %></td>
    </tr>
    <tr>
      <td><%= link_to "関東地方", "/region/kanto" %></td>
    </tr>
    <tr>
      <td><%= link_to "中部地方", "/region/chubu" %></td>
    </tr>
    <tr>
      <td><%= link_to "近畿地方", "/region/kinki" %></td>
    </tr>
    <tr>
      <td><%= link_to "四国地方", "/region/shikoku" %></td>
    </tr>
    <tr>
      <td><%= link_to "中国地方", "/region/chugoku" %></td>
    </tr>
    <tr>
      <td><%= link_to "九州地方", "/region/kyusyu" %></td>
    </tr>
    <tr>
      <td><%= link_to "沖縄地方", "/region/okinawa" %></td>
    </tr>
  </tbody>
</table>