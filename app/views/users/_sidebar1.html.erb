<h2 class="mb-3">User profile</h2>
<% if user_signed_in? %>
  <%= image_tag "no_image.png", size: '100x100' %>
  <p class="mt-2">名前：<%= user.name %></p>
  <p>住んでいる（いた）県：<%= user.prefecture2.name %></p>
  <p>住んでみたい県：<%= user.prefecture1.name %></p>
<% else %>
  <p>まだログインしていません</p>
<% end %>

<%= link_to "新規に投稿する", new_post_path(prefecture_id: @prefecture.id), class: "btn btn-primary btn-block" %>


<table class="table table-striped table-hover mt-4">
  <thead>
    <tr>
      <td><h3>Region</h3></td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= link_to "北海道", "" %></td>
    </tr>
    <tr>
      <td><%= link_to "東北地方", "" %></td>
    </tr>
    <tr>
      <td><%= link_to "関東地方", "" %></td>
    </tr>
    <tr>
      <td><%= link_to "中部地方", "" %></td>
    </tr>
    <tr>
      <td><%= link_to "近畿地方", "" %></td>
    </tr>
    <tr>
      <td><%= link_to "四国地方", "/region/shikoku" %></td>
    </tr>
    <tr>
      <td><%= link_to "九州地方", "" %></td>
    </tr>
    <tr>
      <td><%= link_to "沖縄地方", "" %></td>
    </tr>
  </tbody>
</table>